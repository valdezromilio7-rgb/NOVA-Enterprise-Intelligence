# NOVA Product Specification v0.1

A ProductSpecification is created only from a validation decision whose outcome is `proceed_to_build_review`. It preserves opportunity identity and decision provenance, and records value proposition, scope, acceptance criteria, analytics, and architecture requirements.

This contract is a build-review artifact; it does not itself authorize capital allocation or production deployment.
